# Database Configuration
DB_HOST=db
DB_PORT=5432
DB_USER=example_user
DB_PASSWORD=example_password
DB_NAME=example_db

# JWT Secrets - CRITICAL: Generate strong random secrets for production!
# Generate using: openssl rand -base64 32
# WARNING: All three secrets MUST be different and at least 32 characters
SECRET=CHANGE_ME_use_openssl_rand_base64_32_to_generate
ACCESS_TOKEN_SECRET=CHANGE_ME_use_openssl_rand_base64_32_to_generate
REFRESH_TOKEN_SECRET=CHANGE_ME_use_openssl_rand_base64_32_to_generate

# PgAdmin Configuration (Optional - for database management)
PGADMIN_DEFAULT_EMAIL=user@domain.com
PGADMIN_DEFAULT_PASSWORD=SecurePassword